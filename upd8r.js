var scrapePage = require('./scrapr')
//Updates the quotes array.

var quotesArr = []

function getter(scrapePage, (data, quotesArr) =>
  scrapePage()
  return data
) {
  console.log(data);
  return data
}

getter()
